What is a Router?

A router connects different networks and forwards packets based on IP addresses.

Example:

Laptop
    │
Router
    │
Internet
    │
AWS Server

The router decides the best path for the packet to reach its destination.
